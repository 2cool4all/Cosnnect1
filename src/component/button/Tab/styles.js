import {StyleSheet} from 'react-native';
import { TRANSPARENT } from '../../../utility/colors';

export default StyleSheet.create({
  btn: {
    backgroundColor: TRANSPARENT,
    width: '50%',
    height: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {fontSize: 15, color: '#B2352C'},
});
